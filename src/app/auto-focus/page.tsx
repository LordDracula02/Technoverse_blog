import React from 'react';
import CategoryPage from '../[category]/page';

const AutoFocusPage = () => {
  return <CategoryPage params={{ category: 'auto-focus' }} />;
};

export default AutoFocusPage;
