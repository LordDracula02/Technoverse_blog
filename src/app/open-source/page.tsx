import React from 'react';
import CategoryPage from '../[category]/page';

const OpenSourcePage = () => {
  return <CategoryPage params={{ category: 'open-source' }} />;
};

export default OpenSourcePage;
